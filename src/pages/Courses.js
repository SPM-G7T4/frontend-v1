import React from "react";

const Courses = () => {
  return (
    <h1
      style={{
        width: "100vw",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Courses Page
    </h1>
  );
};

export default Courses;
