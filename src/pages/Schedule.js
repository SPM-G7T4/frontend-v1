import React from "react";

const Schedule = () => {
  return (
    <h1
      style={{
        width: "100vw",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Schedule Page
    </h1>
  );
};

export default Schedule;
